export enum RES_TYPES {
    ERROR = 'error',
    WARNING = 'warning'
}

export enum CODES {
    // Specify the code if you want to handle particularly in front-end like 
    DEFAULT = 1000,
    PERMISSION_DENIED = 1001,
    TOKEN_EXPIRED = 1002
}

export enum USER_TYPES {
    ADMIN = "ADMIN",
    USER = 'USER',
}

export enum EVENT_TYPES {
    VIEW = "VIEW",
    CLICK = 'CLICK',
}

export enum OTP_TYPES {
    SIGNUP = 'SIGNUP',
    LOGIN = 'LOGIN'
}